name: ${PROJECT}
description: ${DESCRIPTION}
runtime: java
template:
  description: "A minimal Google Native Pulumi Kotlin program"
  important: true
  config:
    google-native:project:
      description: The Google Cloud project to deploy into
